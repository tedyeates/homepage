export type CardType = {
    title: string
    summary: string
    image?: string
    imageAlt?: string
    description: string
    faceDown?: boolean
}

export type Point = {
    x: number
    y: number
}